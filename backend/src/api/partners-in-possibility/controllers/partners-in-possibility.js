'use strict';

/**
 * partners-in-possibility controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::partners-in-possibility.partners-in-possibility');
