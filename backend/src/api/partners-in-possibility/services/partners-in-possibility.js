'use strict';

/**
 * partners-in-possibility service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::partners-in-possibility.partners-in-possibility');
